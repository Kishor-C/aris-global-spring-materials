# property to mention the logical name of the microservice
spring.application.name=first-app

# property to specify the auth-server /user endpoint

# we can use zuul api gateway
# security.oauth2.resource.user-info-uri=http://localhost:7071/user
security.oauth2.resource.user-info-uri=http://localhost:6666/auth/user
